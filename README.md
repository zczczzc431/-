This project only contains the main function and libraries—I had to trim it down due to the total file size. The build environment is ESP-IDF 5.5.4 on VS Code. There are two components: END-1 handles sensors and networking, while END-2 is an unstable work-in-progress voice dialogue module built with Doubao. I originally tried to replicate Xiaozhi but failed, so I attempted to build my own instead—though its responsiveness still falls far short.
这个项目里面只有主函数和库哈，因为总文件太大了，编译环境是VScode上的ESP-IDF5.5.4，里面有两个东西应该，一个是END-1负责传感器和网络的，另一个是个半成品END-2不太稳定啊是个语音对话，接豆包的，之前想复刻小智失败了只能自己试着做一个结果流畅性差远了
